name: uv
description: Python package manager and virtual environment tool
dependencies:
  - user  # uv needs user context for proper permissions
auto_detect:
  files:
    - "^pyproject\\.toml$"
    - "^package\\.json$"
    - "^Cargo\\.toml$"
    - "^poetry\\.lock$"
    - "^requirements.*\\.txt$"
    - "^\\.python-version$"
    - "^environment\\.ya?ml$"
    - "^conda\\.ya?ml$"
